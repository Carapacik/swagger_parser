openapi: 3.1.0
info:
  title: API
  version: 1.0.0
paths:
  /pets:
    get:
      operationId: findPets
      parameters:
        - in: query
          name: deprecatedQueryParameter
          deprecated: true
          required: true
          schema:
            type: integer
            default: 0

      responses:
        "200":
          description: List of Pets
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Pet'

components:
  schemas:
    Pet:
      type: object
      required:
        - deprecatedProperty
      properties:
        deprecatedProperty:
          type: integer
          deprecated: true
          format: int64